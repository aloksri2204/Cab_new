// Example configuration file for AkhileshCab website
// IMPORTANT: This is just a template. During deployment, a real config.js with the actual API key will be created.

const CONFIG = {
  // The Google Maps API key will be injected during deployment by GitHub Actions
  GOOGLE_MAPS_API_KEY: "YOUR_GOOGLE_MAPS_API_KEY_HERE",
  
  // Version number for cache busting
  WEBSITE_VERSION: "1.0.0"
};

// For local development:
// 1. Rename this file to config.js
// 2. Replace the placeholder with your actual Google Maps API key
// 3. DO NOT commit the actual API key to the repository