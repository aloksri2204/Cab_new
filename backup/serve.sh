#!/bin/bash
# Simple server to serve the static HTML site

echo "Starting HTTP server for the HTML-only AkhileshCab website..."
echo "The website will be accessible at Replit's WebView"

# Run Python's built-in HTTP server
python -m http.server 8080